import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  positionMenu: {
    left: "-110px !important"
  },
  makeupButton: {
    borderRadius: "10px",
    fontSize: 15
  }
}));
